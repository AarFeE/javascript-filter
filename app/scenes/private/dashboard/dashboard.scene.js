export function DashboardScene() {
    const pageContent = `
        <h1>Dashboard</h1>
        <p>This is the dashboard page.</p>
    `

    const logic = () => {}

    return { pageContent, logic }
}